export interface Summit{
    id: string,
    name: string,
    altitude: number,
    massif: string,
    photoUrl?: string,
    wikiDescription?: string,
    linksUrl?: string[]
    photoGallery?: string[]
}